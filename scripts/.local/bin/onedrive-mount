#!/bin/bash

onedrive_location="$HOME/OneDrive"
[ -d "$onedrive_location" ] || mkdir -p "$onedrive_location"
rclone --vfs-cache-mode writes mount onedrive: $onedrive_location &
notify-send 'OneDrive mounted' "OneDrive has been mounted to $onedrive_location."
