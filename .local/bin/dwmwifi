#!/bin/bash

source config

case $BUTTON in
	1|3) setsid -f st -c Floating -e nmtui;;
esac

wifi=$(iwgetid -r)
[ -z "$wifi" ] && wifi="Disconnected"

[ "$COLORED" = true ] && echo " 📶 $wifi |" || echo " 󰤨 $wifi |"
