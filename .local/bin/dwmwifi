#!/bin/bash

source config

case $BUTTON in
	1) setsid -f st -c Floating -e nmtui;;
    3) nm-connection-editor;;
esac

wifi=$(iwgetid -r)
[ -z "$wifi" ] && wifi="Disconnected"

[ "$COLORED" = true ] && echo " 📶 $wifi |" || echo " 󰤨 $wifi |"
