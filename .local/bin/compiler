#!/bin/bash

file=$1
dir="${file%/*}"
base="${file%.*}"
ext="${file##*.}"

cd "$dir" || exit

compile_md() {
	pandoc \
		"$base".md \
		-s -o "$base".pdf \
		-V 'geometry:margin=1in' \
		--highlight-style=tango \
		--pdf-engine=xelatex \
		--verbose
}

case "$ext" in
	md)  compile_md;;
esac
