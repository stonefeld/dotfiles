#!/bin/bash

source config

per=$(cat /sys/class/power_supply/BAT0/capacity)
char=$(cat /sys/class/power_supply/AC/online)

if [ "$COLORED" = true ]; then
	[ "$char" -eq 1 ] && echo " 🔌 $per% |" && exit
	icon="🔋"
else
	[ "$char" -eq 1 ] && echo " 󰂄 $per% |" && exit
	icon="󰂃"
	bat=$(( $per / 10 ))
	case $bat in
		"0")  icon="󰂎";;
		"1")  icon="󰁺";;
		"2")  icon="󰁻";;
		"3")  icon="󰁼";;
		"4")  icon="󰁽";;
		"5")  icon="󰁾";;
		"6")  icon="󰁿";;
		"7")  icon="󰂀";;
		"8")  icon="󰂁";;
		"9")  icon="󰂂";;
		"10") icon="󰁹";;
	esac
fi
echo " $icon $per% |"
