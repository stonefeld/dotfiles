#!/bin/bash

source dwmconfig

case $BUTTON in
	1) termlaunch -f -g 120x34 -e calcurse;;
esac

# [ "$COLORED" = true ] && date "+ 🕓 %H:%M " || date "+ $(cl 'vi' '')󱑂^d^ %H:%M "
# [ "$COLORED" = true ] && date "+ 🕓 %H:%M " || date "+ 󱑂 %H:%M "
date "+%H:%M "
