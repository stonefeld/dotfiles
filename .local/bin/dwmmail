#!/bin/bash

case $BUTTON in
	1) mailcheck &;;
esac

unread="$(find "${XDG_DATA_HOME:-$HOME/.local/share}"/mail/*/[Ii][Nn][Bb][Oo][Xx]/new/* -type f | wc -l 2>/dev/null)"
pgrep -x mailcheck >/dev/null && sync="  " || sync=""
echo " ^a^[   ^n^$unread^a^$sync ]"
