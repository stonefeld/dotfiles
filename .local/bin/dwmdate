#!/bin/bash

source config

case $BUTTON in
	1) termlaunch -f -g 150x42 -e calcurse;;
esac

# [ "$COLORED" = true ] && date "+ 📅 %a, %b %d (%Y) |" || date "+ $(cl 'or' '')󰃮^d^ %a, %b %d (%Y) |"
[ "$COLORED" = true ] && date "+ 📅 %a, %b %d (%Y) |" || date "+ $(cl 'ye' '')󰃮 %a, %b %d (%Y) |"
