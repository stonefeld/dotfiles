#!/bin/sh

menu="$1"

if [ "$menu" = "drun" ]; then
    rofi -show drun -theme applauncher
elif [ "$menu" = "powermenu" ]; then
    rofi -modi 'Powermenu:rofipowermenu' -show Powermenu -theme powermenu
fi
