#!/bin/sh

COLORED_EMOJIS=true

recording=""
[ -f /tmp/recordingpid ] && ([ ! "$COLORED_EMOJIS" = true ] && recording="  | " || recording="🎤 | ")
pacupgrade=$(cat $XDG_DATA_HOME/updates)

wifi=$(iwgetid -r)
[ -z "$wifi" ] && wifi="Disconnected"

brightness=$(status-brightness $COLORED_EMOJIS)
volume=$(status-volume $COLORED_EMOJIS)
battery=$(status-battery $COLORED_EMOJIS)
date=$(status-clock $COLORED_EMOJIS)

[ ! "$COLORED_EMOJIS" = true ] &&
    statusline="$recording 0 -  $pacupgrade -  $wifi - $brightness - $volume - $battery - $date" ||
    statusline="$recording📧 0 📦 $pacupgrade | 📶 $wifi | $brightness | $volume | $battery | $date"

xsetroot -name "$statusline"
