#!/bin/sh

volume=$(pacmd dump-volumes | sed "1!d" | sed -n "s/ \+/ /gp" | cut -d " " -f 8)
icon=""
mute=$(amixer | grep "Front Left:" | sed -n "s/ \+/ /gp" | cut -d " " -f 7)
[ $mute = "[off]" ] && volume="Mute" && icon=""

echo "$icon $volume"
