# System variables control
# "current=$(pacmd dump-volumes | sed '1!d;s/ \+/ /g;s/\%//' | cut -d ' ' -f 8) && [ $current -lt 200 ] && pactl set-sink-volume @DEFAULT_SINK@ +10% && status-init"
# "amixer sset Master 2+ && status-init"
"pactl set-sink-volume @DEFAULT_SINK@ +10% && status-init"
    XF86AudioRaiseVolume
# "amixer sset Master 2- && status-init"
"pactl set-sink-volume @DEFAULT_SINK@ -10% && status-init"
    XF86AudioLowerVolume
# "amixer sset Master toggle && status-init"
"pactl set-sink-mute @DEFAULT_SINK@ toggle && status-init"
    XF86AudioMute
"xbacklight -inc 10 && status-init"
    XF86MonBrightnessUp
"xbacklight -dec 10 && status-init"
    XF86MonBrightnessDown
"playerctl --player=spotify play-pause"
    Pause

# Utilities
"touchpad"
    Shift + F6
"rfkill unblock all && [ $(pgrep -x nm-applet) ] && killall nm-applet || nm-applet"
    Shift + F7
"screenshot"
    Print
"screenshot 5"
    Shift + Print
"screenshot -s"
    Shift+Mod4 + s

# Launch applications
"dmenu_run -c -l 20 -bw 2 -i"
    Mod4 + p
"$BROWSER"
    Shift+Mod4 + i
"passmenu -c -l 20 -bw 2 -i"
    Shift+Mod4 + k
"st -c Floating -e pulsemixer"
    Shift+Mod4 + p
"st -c Floating -g 120x34 -e music"
    Mod4 + o
"st -c Floating -g 120x34 -e htop"
    Mod4 + i
"galculator"
    Mod4 + u
"st -c Floating -g 120x34 -e vifm"
    Mod4 + e
"st -c Floating -g 120x34 -e newsboat"
    Shift+Mod4+ n
"dmemoji"
    Shift+Mod4 + e
"dmlogout"
    Shift+Mod4 + l
"dmrecord"
    Shift+Mod4 + r
"klayout ; status-init"
    Mod4 + a
"klayout inverted ; status-init"
    Shift+Mod4 + a
