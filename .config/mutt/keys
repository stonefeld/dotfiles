bind index <esc>V noop
bind index <esc>v noop
bind index \Cn noop
bind index \Cp noop
bind alias,index,pager,browser,query g noop
bind pager q noop

bind index j next-entry
bind pager j next-line

bind index k previous-entry
bind pager k previous-line

bind pager l view-attachments
bind index l display-message
bind attach l view-mailcap
bind browser l select-entry

bind index \\ limit

bind index h noop
bind pager,attach h exit
bind browser h goto-parent
bind index,pager H view-raw-message

bind index S sync-mailbox

bind alias,index,browser,query G last-entry
bind pager G bottom

bind alias,index,browser,query gg first-entry
bind pager gg top

bind alias,index,pager,browser,query d half-down
bind alias,index,pager,browser,query u half-up

bind index D delete-message
bind index U undelete-message

bind index <esc><space> collapse-all
bind index <space> collapse-thread

bind index n search-next
bind index p search-opposite

bind index { previous-thread
bind pager { half-up

bind index { next-thread
bind pager { half-down

bind index,pager a group-reply

bind index,pager \Cj sidebar-next
bind index,pager \Ck sidebar-prev
bind index,pager \Co sidebar-open
bind index,pager B sidebar-toggle-visible

bind editor <tab> complete-query

# vim: ft=neomuttrc
